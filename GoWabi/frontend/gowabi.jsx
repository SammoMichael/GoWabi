import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
    console.log('ready')
    const root = document.getElementById('root');
    ReactDOM.render(<h1>Welcome to GOWABI</h1>, root);
});
